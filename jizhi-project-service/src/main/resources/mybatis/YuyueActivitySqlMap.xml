<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="yuyueActivity" >
  <resultMap id="BaseResultMap" type="com.jizhi.model.YuyueActivity" >
		<id column="id" property="id" jdbcType="VARCHAR" />
		<result column="open_id" property="openId" jdbcType="VARCHAR" />
		<result column="name" property="name" jdbcType="VARCHAR" />
		<result column="phone" property="phone" jdbcType="VARCHAR" />
		<result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
		<result column="deadLine_time" property="deadLineTime" jdbcType="TIMESTAMP" />
		<result column="yard_id" property="yeadId" jdbcType="INTEGER" />
		<result column="status" property="status" jdbcType="INTEGER" />
  </resultMap>
  <sql id="Base_Column_List" >
	id,open_id,name,phone,create_time,yard_id,status,deadLine_time
  </sql>
  
  <insert id="insert" parameterType="com.jizhi.model.YuyueActivity">
  	insert into yuyue_activity_#{tbindex} (id,open_id,name,phone,create_time,yard_id,status,deadLine_time) 
  		values (#{id},#{openId},#{name},#{phone},#{createTime},#{yeadId},#{status},#{deadLineTime})
  </insert>
 
  <select id="queryByOpenId" resultMap="BaseResultMap" parameterType="java.util.HashMap">
  	select 
  	<include refid="Base_Column_List"></include>
  	from yuyue_activity_#{tbindex} where open_id = #{openId} 
  	<if test="status > 0 " > and status=#{status}</if>
  	order by create_time desc
  	limit #{begin},#{size}
  </select>
  
  <update id="updateStatus" parameterType="com.jizhi.model.YuyueActivity">
  	update yuyue_activity_#{tbindex} set status = #{status} where open_id = #{openId} and id = #{id}
  </update>
  
  <select id="queryById" resultMap="BaseResultMap" parameterType="java.util.HashMap">
  	select 
  	<include refid="Base_Column_List"></include>
  	from yuyue_activity_#{tbindex} where open_id = #{openId} and id = #{id}
  </select>
  
</mapper>