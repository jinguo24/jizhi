<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="coupon" >
  <resultMap id="BaseResultMap" type="com.jizhi.model.Coupon" >
		<id column="id" property="id" jdbcType="INTEGER" />
		<result column="phone" property="phone" jdbcType="VARCHAR" />
		<result column="money" property="money" jdbcType="INTEGER" />
		<result column="create_time" property="createTime" jdbcType="DATE" />
		<result column="status" property="status" jdbcType="INTEGER" />
		<result column="use_time" property="useTime" jdbcType="DATE" />
		<result column="expir_date" property="expirDate" jdbcType="INTEGER" />
  </resultMap>
  <sql id="Base_Column_List" >
	id,phone,money,create_time,status,use_time,expir_date
  </sql>
  
  <insert id="insert" parameterType="com.jizhi.model.Coupon">
  	insert into coupon_#{tbinedex} (phone,money,create_time,status,use_time,expir_date) 
  		values (#{phone},#{money},#{createTime},#{status},#{useTime},#{expirDate})
  </insert>
  
  <update id="updateStatus" parameterType="com.jizhi.model.Coupon">
  	update coupon_#{tbinedex} set status = #{status} where id=#{id} 
  </update>
  
  <select id="query" resultMap="BaseResultMap" parameterType="java.util.HashMap">
  	select 
  	<include refid="Base_Column_List"></include>
  	from coupon_#{tbinedex} where phone = #{phone}
  	<if test="status > 0" > and status = #{status} </if>
  	order by id desc limit #{begin},#{size}
  </select>
  
  <select id="queryById" resultMap="BaseResultMap" parameterType="java.util.HashMap">
  	select 
  	<include refid="Base_Column_List"></include>
  	from coupon_#{tbinedex} where id = #{id}
  </select>
</mapper>